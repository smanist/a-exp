layout_version: 1
default_project: a-exp
kit:
  mode: local
  source: .
scheduler:
  add_defaults:
    name: work-cycle
    cron: "0 * * * *"
    message_default: true
    model: strong
    cwd: "."
    max_duration_ms: 1800000
